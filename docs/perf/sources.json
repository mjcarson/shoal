{
  "version": 1,
  "note": [
    "Which sources each measurement layer is considered to measure.",
    "",
    "shoal-bench hashes these to decide whether a committed capture still describes the current",
    "code. A path may be a file or a directory; a directory means every .rs file beneath it,",
    "walked and sorted.",
    "",
    "This list is a hand maintained approximation and it will drift. The staleness rule is built",
    "so that drifting is safe in one direction only: the commit hash is checked first and catches",
    "every change coarsely, and these digests can only ever narrow a 'stale' verdict to",
    "'unaffected'. They can never turn 'stale' into 'fresh'. A path missing from this file",
    "therefore produces a capture wrongly reported as unaffected by a change, never one wrongly",
    "reported as fresh - and an unaffected verdict still shows how many commits have landed since.",
    "",
    "Adding a path here changes the manifest hash, which is recorded in every capture taken",
    "afterwards, so the change is visible rather than silent.",
    "",
    "F8 moved the workloads out of shoal/examples/tmdb.rs and into shoal-bench/src/workloads, and the stage report builder out of shoal/src/stages.rs into the same place. The three workload layers point at that directory now. This changes the manifest hash, so every capture taken before F8 is correctly reported as no longer describing the current macro layer - which is true, and is not something to engineer around.",
    "",
    "F10 added shoal/benches/wire.rs and the protocol module it measures to the micro layer. The client is deliberately not listed here: wire.rs exercises the framing, not the client, and shoal-core/src/client.rs is already in the macro layer. This changes the manifest hash, so every micro capture taken before F10 is correctly reported as no longer describing the current micro layer.",
    "F27 added shoal-proto/src/shared/responses.rs and row_ref.rs to the micro layer. wire_codec/response/* archives and reads back the response types, and responses.rs was never listed - so a capture would have been reported as unaffected by the change that rewrote the thing it measures. That is the same drift F15 caused, arriving the other way round: not a listed path that stopped existing, but a measured file that was never listed. The test guarding this file only checks that the paths it names resolve, which cannot see a file that is missing from it. Filed as the general gap in known-issues; the two paths are added here.",
    "",
    "F15 split the crates and six of the seventeen paths below stopped existing: the client moved to shoal-client/src/, and shared/ - including the protocol module wire.rs measures - moved to shoal-proto/src/. A missing path is silently dropped, so the micro layer was hashing two benches and one source rather than the protocol module it is half about, and the macro layer was not hashing the client at all. That is the drift this file's own note says is safe in one direction: those captures were reported as unaffected by changes that did affect them. Corrected here, together with F24 adding shoal/benches/routing.rs and the routing and ring sources it measures. Both changes move the manifest hash, so every capture taken before this is correctly reported as no longer describing the layers it names."
  ],
  "layers": {
    "micro": [
      "shoal/benches/partitions.rs",
      "shoal/benches/wire.rs",
      "shoal/benches/routing.rs",
      "shoal-core/src/server/tables/partitions.rs",
      "shoal-core/src/server/ring.rs",
      "shoal-core/src/server/routing.rs",
      "shoal-proto/src/shared/protocol.rs",
      "shoal-proto/src/shared/protocol",
      "shoal-proto/src/shared/responses.rs",
      "shoal-proto/src/shared/row_ref.rs"
    ],
    "macro": [
      "shoal-bench/src/workloads",
      "shoal-core/src/server.rs",
      "shoal-core/src/server",
      "shoal-client/src/client.rs",
      "shoal-client/src/client",
      "shoal-proto/src/shared.rs",
      "shoal-proto/src/shared"
    ],
    "hotpath": [
      "shoal-bench/src/workloads",
      "shoal-core/src/server.rs",
      "shoal-core/src/server"
    ],
    "stages": [
      "shoal-bench/src/workloads",
      "shoal-core/src/server/stage_profile.rs",
      "shoal-core/src/server.rs",
      "shoal-core/src/server"
    ]
  }
}
