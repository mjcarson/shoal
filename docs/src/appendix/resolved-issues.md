# Resolved Issues

Defects that have been fixed, kept because the reasoning behind a fix is worth more than
the fix itself. Each page states what went wrong, why, what shape the fix took, what was
rejected on the way, and — the part that matters when changing this code later — the
invariants the fix depends on.

Item numbers are shared with [Known Issues](known-issues.md) and never reused, so a number
appears on exactly one of the two pages. The exceptions are items 15, 16, 20, 24, 32, 54, 69 and 91, which were
only partly fixed and appear on both: the fixed half here, the open remainder there. Items 9 and 51
were each one of those exceptions until their second half was fixed, and now appear here alone.

Numbers are also grouped when one change closed several items that turned out to share a cause —
7 and 10, 26 and 39, 56 and 61, 67 and 68, and 11, 12 and 37 each have one page rather than
several.

| # | Issue | What fixed it |
| --- | --- | --- |
| 1–3 | [Acknowledged writes were not durable](resolved/durability.md) | A rewritten intent log: real commit positions, a contiguous flush watermark, `fdatasync` before acknowledgement, block aligned writes |
| 4 | [Unsorted updates and deletes never consult disk](resolved/unsorted-disk-consultation.md) | Both now park on `blocked` and replay once the partition is loaded, and an unsorted delete leaves a tombstone behind |
| 5 | [Deleted rows came back](resolved/resurrected-deletes.md) | `MapIntent::Remove` for pruned partitions, and a real flushed generation so a tombstone is never evicted before its delete has been compacted |
| 6 | [Memory accounting collapsed to zero on a partition load](resolved/memory-accounting.md) | A merge recomputes its size instead of inheriting the archive extent's, and the signed adjustment stopped going through a cast to `usize` |
| 7, 10 | [`limit` was ignored by persistent sorted tables](resolved/sorted-limit.md) | One limit-aware scan shared by both partition forms, counted against accumulated rows; queries narrowed per shard instead of broadcast whole; and the splitting shard merges the shares so a limit spans shards |
| 8 | [Sort keys were accepted and ignored](resolved/sort-keys.md) | Both scans seek their named keys instead of walking — in memory and in an archive — `exists` moved onto the same shared pair, and the keys are sorted and deduplicated once as a query enters the server |
| 9 | [Orphaned update intents panicked, then were dropped silently](resolved/orphaned-update-intents.md) | `apply_intents` seeds from the archive copy and both sites warn and skip instead of panicking; then a `RecoveryStats` counted every drop, separated loss from a correctly dropped update, and reported it once per shard at the end of startup |
| 11, 12, 37 | [The ring panicked on an empty lookup and never smoothed load](resolved/tablet-ring.md) | A tablet map replaced the vnode ring: ownership is stored per tablet instead of hashed, the map is built complete from the shard count before any shard starts so an empty or partial one cannot exist, tablet ids come from the high bits so a later split stays incremental, and `add` became idempotent |
| 13 | [Eviction logging can underflow](resolved/eviction-log-underflow.md) | The subtraction moved into a total `eviction_totals`, and the event reports what the pass actually dropped alongside what the counter moved by, so drift is measured rather than assumed away |
| 14 | [Empty rotated intent logs were never deleted](resolved/empty-rotated-logs.md) | The removal moved out of the arm that had partitions to write, so a rotation that compacted nothing cleans up after itself, and a log the reader could read nothing from is deleted with a warning rather than silently |
| 16, 51 | [A partition read that failed panicked its shard and stranded its queries](resolved/partition-load-failure.md) | The read moved into its own task, failures classified by whether reading again could answer differently, and a `PartitionLoadFailed` message so the queries parked on a read are released whether it succeeded or not — each marked to answer without the read that just failed, so the replay cannot ask for it again |
| 17 | [Leftover debug `println!`s](resolved/leftover-printlns.md) | Gone in four parts over four changes: the six `exists` lines and the compaction line first, the trace-sink line with [Resolved #90](resolved/divergent-layer-filters.md), and the `listening on` line — printed *before* the bind it announced, by a function called to format a string — with [Resolved #38, 58, 88](resolved/pool-readiness.md), once `ShoalPool::ready` reported the address every shard actually bound and no test needed to read stdout for it. `shoal-core/src` prints nothing to stdout now; the check is a `grep`, quoted on the page |
| 18, 50 | [Core exclusion was silently ignored, and shard placement was random](resolved/excluded-cores-typo.md) | `Resources` became `deny_unknown_fields` so a misspelled key fails the load instead of being dropped; and `Resources::cpus`, which was taking cpus off a `HashSet` in hash-seed order, now sorts by topology and fills distinct physical cores before it pairs onto an SMT sibling |
| 20 | [The storage tests were entirely commented out](resolved/storage-tests.md) | Rewritten against explicit on-disk fixtures and turned back on |
| 38, 58, 88 | [The pool returned before its shards had bound, and never said what became of them](resolved/pool-readiness.md) | Three symptoms of one gap. `ShoalPool::start` spawned its shard threads and returned with their join handles and nothing else, so a dead shard was reported to nobody (58 — **established by reproduction**: a pool started on a held port returned `Ok` and a client reached the holder), every test chose a port from a per-binary counter and slept two seconds (38), and the benchmark's probe raced the bind and logged a dozen refusals per workload (88). [F36](../features/cluster-harness.md) gave every shard a channel to the pool: `Ready` with its bound address once `Shard::init` returns, `Failed` with the error if construction or the loop fails, under an id minted in the spawn closure so a failure in `Shard::new` still has a name. `ready(timeout)` waits for all or reports the first by name, `failure()` asks afterwards, `bound_addr()` says where, and `exit` returns the first shard error from either half of a join result. Port zero is resolved before the shards spawn by a `SO_REUSEPORT` reservation the pool holds — bound, never listening, so it takes no connection — until every shard has bound the same port. Every helper starts on zero and waits on `ready`; the counter and the `listening on` line are gone. The honest `exit` surfaced [item 91](resolved/compaction-retry.md) on its first full run |
| 54 | [`#[shoal::db]` needed crates the caller had never heard of](resolved/macro-emits-three-crates.md) | The macros emit `::shoal::` and nothing else, and the facade re-exports each crate from the one whose trait signatures name it; and [F15](../features/client-server-split.md) removed glommio from a client's graph rather than re-exporting it. `rkyv` and `deepsize2` remain, because a schema writes those derives by hand |
| 67, 68 | [Chart labels collided, and the scope prefix strip never matched](resolved/chart-labels.md) | Label placement moved out of the loop that draws the curves, so every position is known before any is chosen; and `shorten` strips the prefixes scopes actually carry rather than one no capture has contained |
| 24 | [A bad query could leave the terminal in raw mode](resolved/shoalctl-panic.md) | The parse error is recorded instead of panicking past `ratatui::restore()`; ~~and rendered~~ — nothing drew it until [item 48](resolved/query-error-display.md) |
| 25 | [`CLAUDE.md` described a Shoal that no longer existed](resolved/claude-md-drift.md) | The two remaining false claims corrected in the file itself — `Conf::new` became `Conf::from_file`, and "LRU eviction at 60%" became what the shard actually does, which is trigger on the limit being exceeded and then free 40% of current usage |
| 26, 39 | [A multi-partition get answered in an arbitrary order](resolved/partition-order.md) | `IN` and same-field `OR` replaced an `AND` that meant three different things; rows are slotted per partition on each shard and reordered by the coordinator before the limit is applied |
| 31 | [Multi-log recovery discarded already-replayed intents](resolved/multi-log-recovery.md) | One prescan across every log before any replay, so no load can overwrite a partition an earlier log replayed into; each partition loaded once; inactive logs deleted only once every replay has succeeded |
| 34 | [The request length prefix is unvalidated](resolved/unvalidated-length-prefix.md) | An eight byte header carrying a version, a message type, flags and a `u32` length, checked in a pure decoder in front of the allocation it used to feed, against a `max_frame_bytes` the two peers exchange when a connection opens — and five `panic!` sites in the two relays turned into a logged `break`, so a frame nobody can read now ends one connection instead of the shard and every client on it |
| 44 | [A compaction discarded a damaged log's tail in silence](resolved/compaction-tail-loss.md) | The report moved out of the arm that had nothing to compact and down beside the delete it describes, split into three named outcomes by `classify_tail`, and `truncated_logs` now reaches the compaction path's counters |
| 45 | [The storage marker's format field was written and never read](resolved/storage-marker-format.md) | `claim` refuses a marker whose `format` it does not know, and refuses on it before reading the shard count out of a layout it cannot interpret |
| 48 | [A query that does not parse was answered with silence](resolved/query-error-display.md) | The error keeps the span the parser gave it, an `Error` box under the query says what went wrong, and the part of the query it blames is drawn red and underlined — as a style on the cells the query already occupies, never as characters that could be read back as query text |
| 56, 61 | [A response cannot say that a read failed](resolved/response-error-channel.md) | A `ResponseAction::Error` carrying a pinned code, and a frame level `Error` type for the failures that cannot be a response at all — so a read that could not be made stops arriving as an empty partition, a response too large to frame is answered instead of closing the connection, and a connection whose reader stopped tells the queries it owed rather than leaving them waiting on a channel that never closes |
| 57 | [A missing archive was created empty rather than reported](resolved/missing-archive.md) | `get_archive` stopped opening with `create(true)` and now reports a `ShoalError::ArchiveMissing` naming the archive and its path, classified `Fatal` so it takes the release path a failed read already had — instead of creating an empty archive whose short read failed much later as corruption, ending the shard and leaving the empty file behind for every later read to find |
| 73 | [Two instrumented runs wrote to one artifact, and the last one won](resolved/stage-artifact-overwrite.md) | The stage layer's runs write to a scratch file named after the workload, and `collect` folds them into a `StageReports` keyed by the workload each report names itself with — so the layer can profile the row axis at three widths instead of keeping whichever ran last. `STAGED_WORKLOADS` split from `PROFILED_WORKLOADS` so pointing one attribution layer at three workloads no longer silently triples the other, and `StageReports::read` still accepts the single-report shape, which is what keeps nine committed captures rendering. **The hotpath half is not fixed** and stays on [Known Issues](known-issues.md) under the same number |
| 74 | [A configuration knob the page did not name disappeared from it](resolved/conf-knob-dropped.md) | `in_order` kept only the knobs its hand-maintained reading order named, while its own doc comment said anything unnamed was appended rather than dropped — so a sweep added to `macro/conf/` was captured at full cost and then silently left off the one page claiming to say what every setting is worth. Split into `in_order` for a section, which must take only its own half, and `in_order_with_rest` for the recommendation table, which takes everything; and the match became a prefix check so a sweep repeated at another row width sorts beside the sweep it repeats |
| 76 | [The stage layer joined nothing for any grid arm](resolved/stage-join.md) | The client half of a stage record was built inside `drive_with` and nowhere else, so every workload whose measured phase ran through one of the per query drivers wrote a report with the right schema, the right name and an empty middle. The bookkeeping moved into a `StageLog` that `Measurement` holds unconditionally and every driver calls, `Shoal::send_one_stamped` gave the one shot path the client stamps it never had, and `stage_profile::reset()` — which had no call site anywhere — now discards the seed phase before the measured one starts. `collect::stages::check` judges each report on its own join instead of the artifact's sum, which is what let one workload's 200,000 carry three zeros past it |
| 78 | [Six of the paths deciding whether a capture is stale did not exist](resolved/sources-manifest-drift.md) | `shoal-bench status` decides whether a committed capture still describes the current code by hashing the sources each layer measures, from a hand-maintained list. [F15](../features/client-server-split.md) moved the client into `shoal-client` and the wire format into `shoal-proto` and the list did not move with it, so **six of its seventeen paths named files that no longer existed** — including `protocol.rs`, which is half of what the micro layer's `wire.rs` measures. A missing path is skipped silently, so eight captures were reported as *unaffected* by changes they were affected by. The paths are corrected, `shoal/benches/routing.rs` and the routing sources [F24](../features/routing-benchmarks.md) added are registered, and a test now asserts every path in the file resolves — which is what would have caught it the day F15 landed |


## How a fix gets written down

Not every bug earns a page. These do because each one had a wrong mental model behind it, and
a page that only said "fixed in commit X" would let the same mistake back in. The sections are
always the same:

**Symptom** what was observable. **Cause** the code as it was. **Evidence** how it was
established — read, or reproduced, and the difference is stated. **The fix** and **Alternatives
rejected**, together, because a fix is only understandable next to what it is not.
**Invariants to uphold**, which is the section to read before changing the code it describes.
**Tests**, naming what fails if the fix is reverted.
| 79 | [A capture of one layer decided what every page drew](resolved/micro-only-capture-current.md) | `render` chose one capture for all eleven pages — the newest with a micro layer — so a `--layer micro` capture emptied the seven macro pages, which then said nothing had ever measured a mixture while eleven macro captures sat in the same directory. Reproduced against the committed corpus with `render --current inline-probe`, which takes `grid.md` from 59,287 bytes to 3,459. The default is now resolved **per layer** and screened on `LayerRecord::complete`, so each page names the newest *whole* capture of the layer it draws and a filtered capture never outranks a full one. `--current` stays an instruction. No committed page moves, because `f24-routing` produced all four layers complete. The converse — [item 77](known-issues.md), a macro-only capture having no way to say it re-measured rows a page is drawing stale — is **not** closed by this |
| 20 (the other half) | [Orphaned source files](resolved/orphaned-sources.md) | `cursor.rs` and `response.rs` were never declared in `server.rs`, so nothing compiled them and nothing warned. The entry kept `response.rs` deliberately, because it defined a response that *borrowed* its rows and was therefore prior art for [O2](optimizations.md) — and warned that whoever started O2 would rediscover it with no way to tell an abandoned design from an unfinished one. Opening it while starting O2 answered that, the other way round: the whole file is twenty-eight lines of struct, `with_capacity` and `PhantomData`, with no method that reads a row and no lifetime to discharge. The prior art was a name and a field. Both files deleted by [F27](../features/grouped-responses.md), which borrows through `RowRef` instead — a design with no relationship to this one beyond both containing an ampersand |
| 80 | [A sorted partition that was never on disk asked storage about it on every get](resolved/never-flushed-partitions.md) | `block_on_load` asked storage to load a partition and, when told there was no archive, returned without recording it. `check_disk` therefore stayed set for the life of a partition that had only ever been written to, so every get of it made a lookup that could only fail — and, worse, was refused the borrowing path [F27](../features/grouped-responses.md) built, since a partition that might have rows on disk may not be answered in place. **The sorted half of [O2](optimizations.md#o2-every-returned-row-is-copied-at-least-twice) was built, tested and unreachable.** The answer is now recorded, and `insert` and `update` clear the same flag when they turn an archive into a loaded partition, the way `delete` already did. Reproduced by counting the lookup spans six gets over two never-flushed partitions open: six before, two after |
| 82 | [Every selected workload was folded into one line per capture](resolved/one-line-per-capture.md) | The explorer's `Source::series` had one loop for a sweep and it was over captures, so every ticked workload contributed a point to the same vector — a chart of one capture was one line however many workloads were on it. Its own opening view drew twenty-four grid arms across four tables as a zigzag through six mixtures, where [the grid page](../performance/grid.md) draws four lines. Ticking a smoke arm beside a full one joined a rate over two thousand rows to one over two hundred thousand; ticking a per-batch percentile beside a per-query one joined a queueing delay to a service time. **Both of those were refusals the code documented and never performed** — the doc comments on `ScaleFactsLite::scale` and `Timing` each said `Source::series` refused to, and neither field was read anywhere in `shoal-top`. Reproduced first: three tests against the unfixed tree, one of which reported `expected one curve per table, got ["one"]`, while all six existing tests passed unchanged — nothing had ever asked how many series a multi-workload selection produces. Fixed by [F30](../features/plot-axis-units.md): units decide what may share a chart, a curve key decides how a legitimate selection is drawn, and `rows` and `keys` are kept out of that key because a workload derives them from the row width rather than setting them |
| 83 | [The explorer opened on a metric that half its corpus could not answer](resolved/default-metric-half-the-corpus-cannot-answer.md) | `Index::units` decided whether a workload could share a chart from the metric's *unit* and the measurement's scale and timing, and never read the measurement — so nothing anywhere asked whether the number existed. [F30](../features/plot-axis-units.md)'s two gates, `picker::refusal` and `Explorer::reconcile`, both inherited the hole, as did `anchor`, `select_all` and `in_one_unit`: six call sites, one missing question. **`Metric::OpsPerSec` is what the explorer opens on and what both presets set, and 88 of the 374 workloads carry no value for it** — every `encryption`, `fanout`, `transport`, `macro/get_*` and `macro/insert_*` arm. They were offered undimmed, ticked cleanly, could set the units for everything after them, and drew nothing, under a strip reading *those captures did not measure that workload* — which was false: the captures measured them, they have no query counter. Meanwhile the metric list crossed **every operation name in the whole corpus** with seven ranks, so at least fourteen of the twenty-eight latency entries were dead for anything selectable and usually twenty-one were. Reproduced against the unfixed tree, which reported `88 workloads are offered on a metric they carry no value for`. Fixed by [F31](../features/metric-availability.md): `units` reads the measurement, which corrects all six call sites at once; the metric control became three lists narrowed to the intersection of what the ticked workloads answer; and the picker hides what it cannot offer while counting it |
| 84 | [Two workloads the recorded facts cannot tell apart drew as one line](resolved/identical-facts-one-line.md) | [Item 82](resolved/one-line-per-capture.md) again, in the case its fix does not reach. A sweep groups arms into curves by `Index::curve_key` - the facts a caller *set* minus the one the axis reads - and two workloads that set none of them differently answer to one key. `macro/get_resident` and `macro/get_archived` are one struct with a `Residency` field that decides which path the workload drives and reaches no field of `ScaleFacts`: both declare 200,000 rows of 256 bytes at a concurrency of sixteen, so ticking both drew a **single vertical segment** joining a get served from memory to a get served off disk, legended `f28-rearchive` with neither workload named. Reproduced before it was fixed, on a fixture whose two identical-fact arms gave `left: ["one"]` - the same line item 82's evidence opens with - and the corpus was then probed rather than assumed, printing the two `ScaleFactsLite` rows field for field. The curve model is not wrong: it answers *which curve is this arm on* and answers correctly. What nothing asked is *would drawing that curve as one line hide something*. [F32](../features/chart-line-identity.md) adds `Index::split_curves` between naming the curves and drawing them: a curve whose members share a position on the key axis becomes **one line per workload**, each named by the identifier that is the only thing separating them, and every other curve is carried through whole so a genuine width sweep is still one line of sixteen points. Putting the identifier in the key instead was rejected against both of item 82's stated invariants - a key must hold no fact that co-varies with the axis, and every key built in one call must be the same length |
| 85 | [The chart stayed framed for the last thing it drew](resolved/chart-framed-for-the-last-selection.md) | Scroll to zoom the explorer's chart once, then move the metric from a rate to a p99, and it draws **empty**: the series are built and handed over correctly and are several orders of magnitude outside a range still framed for the rate. The same for every other change to what is drawn - the key axis, either log toggle, lines-to-bars, ticking a workload whose numbers sit a decade away. `egui_plot` frames a plot on first sight and then keys its `PlotMemory` on the plot's **id**, which here is the constant `"shoal-top-chart"`, so the `auto_bounds = false` that any drag or scroll writes outlives every selection the reader makes afterwards; five sites clear that flag and exactly one restores it, the undocumented double click. Nothing in the crate called `auto_bounds`, `reset` or `set_auto_bounds` at all. **Established by reading the source**, in the crate and in `egui_plot` 0.37, and said so - this machine has no display. The constant id is right and is why this could not fix itself: varying it per selection gives a fresh plot at default bounds and discards the reader's zoom every frame anything moves. [F32](../features/chart-line-identity.md) moves the decision to the only place that can make it. `Explorer::framing` is every input that decides what is on the chart - captures, workloads **in tick order**, metric, key axis, chart kind, both log toggles - compared frame to frame, and when it differs `plot::View::refit` turns `auto_bounds` back on for that one frame. A pan the reader made sticks until the next thing that changes what is drawn; a `fit` button asks for it directly. Hashing the series instead was rejected because a collision is the defect back with nothing to see it by, and noted as where to start when a live server becomes the second `Source` |
| 86 | [The hover label read a logarithmic chart in decades, for a line it could not name](resolved/hover-label-reads-in-decades.md) | `plot::draw` never installed a `label_formatter`, so hovering the explorer's chart ran `egui_plot`'s own default. Three independent faults in one string. The value it prints is `position.y`, which is **plot space** - `plot::runs` takes the logarithm of every value as it builds the points and only the axis formatters undo it - so with `log value axis` on, a curve at 412,500 queries a second reads `y = 5.615`, in no unit and with nothing saying it is a logarithm. The name it prints is the **empty string** for every run of a line after a gap and for every marker overlay, because [F29](../features/benchmark-explorer.md) draws one `Line` per run of consecutive measurements and names only the first, so that the legend keeps one entry per series rather than one per gap - and fifteen hundred of ten thousand possible measurements exist, so gaps are the common case. And it answers for **one line**, the nearest point's, where the question the explorer exists for is which capture is ahead at this key. Fixed in [F33](../features/chart-readout.md) by refusing the label rather than reformatting it: a formatter is handed one item and can never answer for the rest of the chart, and the empty name is not recoverable from inside it. The pointer selects a column and `readout.rs` lists every drawn line's value in it, read from `Series::points` - measurements, not positions - and written with the same `fmt::value` the ticks use. Established by **reproduction**: `egui_plot::default_label_formatter` and `HoverPosition` are both public, so the test calls the exact function the unfixed tree used and keeps the `\nx = 3.000\ny = 5.615` it returned |
| 69 | [Nothing in the workspace ever installed a tracing subscriber](resolved/benchmark-tracing.md) | `shoal-core/src/server/trace.rs` built a complete OTLP over HTTP pipeline - a registry, a batch span processor, an exporter, and a `TraceGuard` whose `Drop` flushes it - and `pub fn setup(conf: &Conf)` had **one caller in the workspace**, `shoal/examples/tmdb.rs`. So the `tracing:` section of `shoal.yml` configured **nothing** for a capture: a file naming a collector behaved exactly like one naming neither a level nor a sink, on every one of the three hundred and seventy-four workloads, and every `#[instrument]` and `event!` the engine and the client reach dispatched to `NoSubscriber` - including the `event!(Level::ERROR, ...)` calls that report a dead connection. `shoal-bench` compounded it from the other end, forcing `conf.tracing.level = Warn` on every workload in `harness/conf.rs`; that line was inert while nothing listened and would have overridden the config the moment something did. **Established by reading the source and confirming by grep**, while writing [F16](../features/client-builder.md)'s *Performance* section - that feature found the client's new spans costing nothing across 144 metrics, and the question *what do these spans cost* turned into *what is listening*. Closed for the benchmark by [F34](../features/benchmark-tracing.md): `trace::setup_with` takes a service name, resource attributes and a console stream; `workload_main.rs` installs it and holds the guard across the run; the override is gone; and `ConfFacts` records `trace_level` and `trace_remote` so `compare` names a pair that traced differently rather than comparing them silently - it read no field of `ConfFacts` at all before. `ShoalPool::start` installing it was rejected on the objection the item itself raised, that a library taking the one global subscriber takes that decision away from every binary embedding it; a `--trace` flag defaulting off was rejected because `--conf` already travels with the capture and a second switch can disagree with it. **`shoalctl`, the tests, and the library question are still open** and keep the item filed on [Known Issues](known-issues.md), the way 16, 20, 24 and 54 are: a test cannot call `setup` at all, because the first one to do so decides what every other test in that binary sees |
| 89 | [One query produced several disjoint traces](resolved/fragmented-query-traces.md) | [Observability](../operations/observability.md) claimed that "spans are propagated manually across channel hops" and that "a trace covers the write, the wait for durability, and the response". Half of it was true: `QueryMetadata.span` really did survive the shard hop, the park on disk and the flush boundary. What broke was everything either side of it. A single get of a partition on disk arrived in **three separate traces** - the query with an unexplained gap where the read was, `Fsloader::spawn_task` on its own, and `loader::read_partition` on its own - because `LoaderMsg::Request` carried `table_name` and `partition_id` and nothing else, `FsLoader::start` is a detached task so the spawn opened at the root of an empty stack, and it then spawned the read into *another* task with no `.instrument()`. The trace also began at `Coordinator::handle_client`, which runs when the coordinator **dequeues** a bundle, so the socket read and the ingress queue were outside every trace; it ended at `Shard::reply`, which hands bytes to a channel, so the framing and the `write_vectored` were in no span at all; and `QueryMetadata::new` took `Span::current()`, which resolved to one span per *bundle*, so a batch of a hundred queries was one flat list of a hundred siblings. **Established by reproduction** - `tracing_topology.rs` was written first and reported `one query's spans resolved to 3 separate traces`, naming each root. The mechanism was checked in the dependencies rather than assumed: a lost parent is not an orphan, it is a **new trace id**, in `tracing`'s `child_of_with` and again in `tracing-opentelemetry`'s `parent_context`. Fixed by rooting the trace at the socket read in `client_rx_relay` and keeping that span alive until the last response for the frame is on the wire; **moving** `Coordinator::send_to_shard`'s span into its loop as `Coordinator::route`, one per query, so the net cost is one registry insert rather than two; carrying a `Span` on `LoaderMsg::Request` (which cost it `Copy`) and through `spawn_task` into the spawned read; and keeping the relay's enter on the query's own span so that span covers routing to response written, which is the server-side latency of one query. A load serves every query parked on its partition, so it is the **child** of the one that asked for it and the rest get a `follows_from` - an OpenTelemetry link - which is the honest shape for a fan in. A `traceparent` on the wire and linking the flush traces to the writes they made durable were both rejected here and filed |
| 90 | [Setting `RUST_LOG` could fragment every exported trace](resolved/divergent-layer-filters.md) | The subscriber's two layers were filtered from two different sources - the console from `EnvFilter::try_from_default_env()`, the OTLP layer from `conf.level.to_filter()` - and both were attached with `.with_filter(...)`, which makes them **per-layer** filters. A per-layer filter decides what a layer can *see*, not only what it emits, so a parent admitted to one and hidden from the other is a parent `tracing-opentelemetry` cannot find; `parent_context` then falls through to `OtelContext::new()`, and an explicit-parent span is not *contextual*, so it does not even get the ambient fallback. Every hop in Shoal's query path uses an explicit parent, because every one of them crosses a channel ([item 89](resolved/fragmented-query-traces.md)), so all of them were exposed. The console output would have looked correct throughout. **Established by reading the source** and said so - the crate documents the hazard in the function it happens in, and no committed config produces the disagreement, so nothing reproduced it. It is worth fixing because the state is reachable by an environment variable and is invisible: `RUST_LOG` is what a person sets when they are trying to work out why their traces look wrong. Fixed by deciding the directives once in `filter_directives` and building both layers' `EnvFilter` from that one string - `EnvFilter` is not `Clone`, which is why a string rather than a filter is what travels. Filtering the registry once instead was rejected because the per-layer split is what lets the OTLP exporter's own `DEBUG` chatter be turned on without turning the whole workspace up. The stray `println!` of the sink configuration went at the same time: it ran before the subscriber existed, so it could never have been an event, and it wrote to the stream a benchmark harvests its hotpath profile from |
| 65 | [Two `gxhash` majors, and partition keys hashed by the one without `deterministic`](resolved/gxhash-pin.md) | The workspace pinned `gxhash 3` with `deterministic` and nothing used it; `shoal-proto` and `shoal-core` pinned `2.2`, resolving to 2.3.1, and hashed every partition key with it. A partition key's hash is a persistence format - it picks the partition and, through the top twelve bits, the tablet - and nothing said which gxhash produced it or held it still, so a major bump would have re-homed every row in silence and two nodes from two lockfiles would disagree about where a row is, which is why [C1](../distributed/node-identity.md#how-it-works) put it before M1. **Established by reading the manifests** and confirmed by `cargo tree`. Fixed by [F37](../features/node-identity-control-plane.md): one workspace pin at `2.3`, both crates taking it, the `deterministic` word gone because it does not exist at 2.x and at 3.x reaches only `GxBuildHasher`, and `partition_keys_hash_to_frozen_values` pinning eight keys of two shapes to the hash and the tablet 2.3.1 gives them - literals obtained by running the test on the unfixed tree, with the lockfile unchanged so nothing was re-hashed. Moving to gxhash 3 was rejected as a migration without a tool. Found on the way and filed: a composite partition key does not compile (item 92), and the archived string hash disagrees with the live one (item 93) |
| 94 | [An answer owed to a client that had left ended the shard](resolved/disconnected-client-cleanup.md) | `reply_sealed` sent every answer with `.await?`, and a `kanal` send fails the moment the connection's write relay is gone - which, since F10, is the moment the client leaves. A client closing with answers owed therefore ended the shard that owed them with `KanalSend(ReceiveClosed)`, and every client on that shard with it; a `panic!` on a missing map entry sat beside it, unreachable only because nothing retired an entry (item 32). **Reproduced**: `client_disconnect.rs` fails on its eighteenth round with the `?` restored. [F38](../features/inter-node-transport.md) made it common - a peer lane is a client to the node it dials, and a lane cut and reconnected leaves answers owed every time. Fixed by treating a dead receiver and a missing entry as an answer with nowhere to go, logged at `DEBUG` and dropped, and by retiring a peer lane's channel from every shard with `ServerMsg::ClientGone` when the lane ends. The ordinary client's retirement - item 32's remainder - is left open, on purpose, until what it costs a disconnect-heavy client has been measured |
| 96 | [`cluster.transport.ping_interval` is parsed, documented and consumed by nothing](resolved/ping-interval-consumer.md) | [F38](../features/inter-node-transport.md) added the field so the `transport` block would keep its shape when the failure detector landed, and nothing read it: the only pings were the fixture's, on demand. **Established by reading the source.** [F39](../features/membership.md) built the consumer, and it is deliberately not the detector: a ping timer on the control thread at `ping_interval` pings every member over the control lane and keeps a local reachability table - round trip and consecutive misses per node - that the `Detector` admin read reports and a member's status report carries to the leader as telemetry. The detector is the leader's phi-accrual over the members' reports at `failure_detector.interval_ms`, a different timer with a different job; a ping never changes membership or health |
| 33 | [Collected split-query state had no expiry](resolved/gather-expiry.md) | A `Gather` was removed from the map only when every share arrived, and nothing bounded the wait: a shard that never sent its share left the gather resident and the client waiting forever, on a standalone node as on a cluster one, since the only sweep that existed expired forwarded queries and ran only beside a peer listener. **Reproduced**: `shoal/tests/gather_expiry.rs` holds every share of a two shard get and, with the expiry absent, the get never returns and the test's own timeout ends it. Fixed by [F41](../features/read-consistency.md): a gather has a slot per share, an attempt per bundle and a deadline from the bundle's budget (`networking.query_deadline`, ten seconds, or a shorter one the bundle names); a share is judged by attempt and slot, so a late or duplicate one is counted and dropped rather than merged; an expired gather is answered `Timeout` once, in the query's own table variant, and forgets its pending forwards before the forward sweep can answer them again; and the sweeper runs on every node. A per-gather timer, an `OutcomeUnknown` answer and a deadline bolted onto the count-down were rejected. Still open: no retry within the budget, and the unbounded local mesh queues of item 15 |
| 99 | [A durable follower's log reversion stopped the leader's whole process](resolved/durable-log-reversion.md) | `allow_log_reversion` was off for a persistent table's groups, and openraft's answer to a disallowed reversion is a `panic!` on the leader's executor, so a member that lost its WAL took every leader it reported to down with it. The reversion is now allowed for every group and the leader feeds the member from its log or a snapshot; the member counts `log_lost` and says so. **Reproduced** by `durable_log_reversion_is_fed_not_fatal`: two leaders panicked and the next write timed out at the handshake |
| 101 | [A member that fell silent before its fifth report was never called `Down`](resolved/short-lived-member-detection.md) | The detector declined to judge a member with fewer than `min_samples` intervals, so one killed young stayed `Up` however long the silence; `fit` now pads the observed intervals with the expected one up to the floor, as a seeded member already was, and `phi` always answers. Reproduced by `down_retains_placement_during_grace` with a view showing three samples, thirty seconds of silence and a phi of zero |
| 104 | [A restart merged every sealed segment below the checkpoint into the archives again](resolved/segments-recompacted-after-restart.md) | `handed` lives in memory, so after a restart every sealed segment between the purge point and the checkpoint was resolved and handed again, and `frames_in` answered every frame in it: the compactor ran the archives backwards through the table's history for the length of the merges. `frames_in` now takes each group's checkpoint and answers only the frames above it. **Reproduced** by `restart_does_not_recompact_segments_below_the_checkpoint`, whose digest on the restarted node read the older generation between two re-merges |
| 105 | [A volatile group never checkpointed, so its log was never purged](resolved/volatile-groups-never-purged.md) | An ephemeral table's group got `checkpoint: None` at open and nothing ever moved it, so the snapshot builder was never offered, openraft never purged, and the memory log grew to `volatile_log_bytes` and shed every write after. The checkpoint now follows the applied position on every apply. **Reproduced** by `a_volatile_group_purges_its_log`: applied 36, checkpoint 0, purged 0, twenty seconds after the writes |
| 108 | [A cluster arm's server overrides were dropped on the way to every node](resolved/cluster-arm-overrides-dropped.md) | `harness::cluster::apply` replaced the block `resolve` built with `Cluster::default()` and set the node's ports, seeds and core on that, so the catch-up arms' retention override and anything else an arm put on its `cluster:` block reached no node, node zero included. **Established by running it**: [F45](../features/replica-migration.md)'s migration arm set `retire_after` to three seconds and its first smoke run recorded the move `unfinished` with every group waiting fifteen seconds at `Activated` for a five minute default grace; with `apply` keeping the resolved block the same run finished the move in seven seconds. `apply` now moves a node's identity on the block it was given and nothing else |
| 111 | [Removing an archive held the handle cache across its close, and a read landing meanwhile panicked the executor](resolved/archive-removal-borrow.md) | `ArchiveMap::remove_archive` took `loaded_archives.borrow_mut()` in the scrutinee of an `if let` whose body awaited the handle's close, so the borrow lived across the await and a partition read reaching `get_archive` meanwhile panicked with "already mutably borrowed". The handle is now taken out in a statement of its own before the await. **Reproduced** by `removing_an_archive_does_not_hold_the_handle_map_across_the_close` against the unfixed tree, after [F47](../features/local-rehome.md)'s crash matrix met it on a node hosting four slots on one executor |
| 97 | [`stage_join.rs` had not compiled since F36, and needed `/opt/shoal` to run](resolved/stage-join-storage.md) | The one server-starting test behind `stage-profile` resolved the committed `shoal.yml`, whose storage is `/opt/shoal`, so on any host without that directory it failed at server start with `PermissionDenied` - and before F38 it had not compiled at all, because a binary behind a feature nobody builds is a binary nobody builds. **Reproduced** on the development host. It now writes a scratch copy of the committed file with its storage under a `tempfile` directory beneath `CARGO_TARGET_TMPDIR`, asserts the server wrote there, and the runbook carries a `cargo check` of both feature-gated binaries |
| 98 | [An admin refusal's error code was derived from its reason text](resolved/admin-refusal-kinds.md) | `ControlResponse::Refused` carried a sentence and `handle_admin` read it: a reason containing `stale version` became `StaleVersion` and every other refusal - not a member, not up, a duplicate, a second initialization, a bad voter count - became `Internal`. **Reproduced** by asserting on the code of a second initialization. `Refused` now carries a `RefusalKind` set at every one of its hundred and thirty-seven sites, `refusal_code` maps the kind to one of nine new codes in the 60-band, and the sentence stays for the log |
| 95 | [`ShoalPool::transport()` reported shard zero's links and called them the node's](resolved/transport-view-every-shard.md) | The loop ran once, over the sender to shard zero, while the pool had held a sender to every shard since the fixture needed to fail one; a four-shard node's artifact showed shard zero's links as the node's. **Reproduced** by a two-shard pool answering `[0]`. It now walks `shard_txs` like `replication()`, one view per shard in shard order, names a shard that does not answer as `ShardFailed`, and the bench record says which shards answered and which shard held each link |
| 102 | [A deferred fixture node could lose its reserved port to an outbound connection](resolved/fixture-port-block.md) | The fixture reserved every node's ports from the ephemeral range with a never-listening `SO_REUSEPORT` socket and dropped the reservations once the started nodes had bound, so a deferred node's ports were free until it started - free for another test's outbound connection, after which it failed to bind `AddrInUse`; and a listener cannot bind over a client-side `TIME_WAIT`, so holding the socket longer only moves the moment. **Established by reading** the reservation code after the one failure under the suite. The fixture now hands out sequential ports from a block at 28 000, below the host's ephemeral floor read from `/proc` and above every port the bench binds, and reserves nothing: the number is the reservation |
| 100 | [`duplicate_node_identity_is_fenced` failed under the fixture suite at full parallelism](resolved/clone-fencing-under-load.md) | A clone at the committed incarnation from other ports is fenced by the state machine when it observes itself - and its observation was gated on its own control group naming a leader, which a member whose loaded vote has no lease and whom the leader never dials stops doing within one election timeout of standing; under load the clone stood first and never asked. **Reproduced** deterministically by holding the first observation back past the election timeout. A recovering member now observes without a leader of its own, and `propose` asks the committed members where the leader is when the local group names none. The suite at thirty-two threads on this host fails on io_uring's probe, not on this |
| 91, 107 | [A compaction that failed ended the compactor, and the tests that met it were read as a get answered by a stale load](resolved/compaction-retry.md) | Every job was `?`: a compaction that met an unreadable archive ended the compactor, the next rotation's send into the closed channel ended the shard, and the two unsorted table tests that inject that fault failed at `pool.exit()` about one run in four - which item 107 read, from the printed error alone, as a second load answering a later get. **Reproduced**, and the reading corrected by a trace: one request and one reply per get, the second load succeeding, the exit failing. A job that fails before it writes is now tried again with a backoff doubling to five seconds, new jobs taken meanwhile; a job that fails after writing still ends the compactor, which is 91's remainder on the known issues page |
| 43 | [The storage marker only guarded the default storage root](resolved/marker-every-root.md) | The claim covered the default latency path alone, so a table under its own `storage.tables` root - or a throughput path apart from the latency one - carried no marker and no lock and could be handed to or taken from another server in silence. **Reproduced**: the table's root had no marker. `Storage::roots` now lists every distinct root, the primary first; each other root is locked and takes a mirror of the primary's marker at the claim, refused by name when it was written by another node, cluster, slot count or layout |
| 110 | [The kill arm's client failed a steady share of its operations for as long as node one was dead](resolved/dead-primary-write-failures.md) | Read as a routing defect - a write whose primary was dead refused rather than proposed through node zero's own replica - and it was the design: every write to a group node one led is `NotLeader` at once, from a follower that cannot commit, until the survivors' lease lapses and they elect, three to four times the failover base, which at the default base is longer than the smoke arm's kill. **Reproduced** with the codes the timeline now keeps: 1,780 `NotLeader` in the `during` window. No change to the write path; every failed sample carries its code, every window `errors_by_code`, and a fixture test pins the refusal and the election at base one |
| 103 | [A returning leader was refused its own re-election until its old lease lapsed, and hops to it waited](resolved/returning-leader.md) | A leader restarted inside its lease stood at once - openraft demotes its loaded self-vote and leaves it a zero lease - and was refused by every follower whose lease of it ran, bumping the term each time, while a write hopping to it from a node with a stale hint landed on its electing member and waited the forwarded deadline out. **Reproduced**: writes held for two to three seconds, the term up by two. A hop onto an electing member is `NotLeader` at once, a member restarted with a vote naming itself waits one lease before standing, and the report carries the term |
| 109 | [A volatile group's survivor tripped an openraft debug assertion when a majority lost its memory log at once](resolved/volatile-majority-loss.md) | A restarted volatile copy looked new to openraft, so the primary initialized the group again and the two empties elected each other over the survivor, whose committed entries the new leader's appends then conflicted with. **Reproduced**: a group left at its old term under a restarted leader, no election for twenty seconds. A shard marks every volatile group it holds under `wal/Shard-N/volatile/`; an empty copy of a marked group neither initializes the group again nor grants a vote to a candidate as empty, for two election timeouts, so the survivor leads and feeds it; and the sweep probes every core and reports one that died |
| 106 | [A member isolated on every lane long enough to inflate its term tripped an openraft debug assertion when healed](resolved/isolated-member-term-inflation.md) | Nothing stopped a member standing when it could reach nobody, so an isolated node's control member and every tablet group on it bumped their term once per election timeout - fifty-one in twelve seconds - and the heal pushed the survivors' term and their leader with it, once into an engine assertion. **Reproduced** by the term. A member whose every link on a lane is down stops standing until one comes back, judged on the control loop's ticks and every shard's, and the topology view carries the control term |
| 15 | [No backpressure anywhere - the shard mesh half](resolved/shard-mesh-admission.md) | Every channel was unbounded and nothing throttled a client, so a shard that fell behind grew its queue until the process was killed. **Reproduced**: two hundred gets to a held shard, none shed, every one waiting the hold out. The mesh stays unbounded - a blocking send between shards is a deadlock - and the bound is at admission: a query bound for another local shard whose queue holds `networking.max_queued_queries` is answered `Shedding` at once and never enqueued, the client retries it, and the transport view counts it. The channels bounded by admitted work are the remainder, still filed |
