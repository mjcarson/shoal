# Delivered Features

Capabilities that have been built, kept for the same reason [Resolved Issues](../appendix/resolved-issues.md)
are kept: the reasoning behind a feature is worth more than the feature. Each page states what
the thing does, the choices its design rests on, what was rejected on the way, what it still
cannot do, and — the part that matters when changing this code later — the invariants it depends
on.

Item numbers are prefixed `F` and are **never reused**, the same rule [Known
Issues](../appendix/known-issues.md) and [Optimizations](../appendix/optimizations.md) follow.

| # | Feature | What it does |
| --- | --- | --- |
| F1 | [Sort-key range predicates](sort-key-ranges.md) | A sorted get or exists can bound its rows by a range of sort keys instead of naming them, in memory and in an archive, which makes an exclusive lower bound a cursor and paging a large partition cost a page |
| F2 | [Projections on get queries](projections.md) | A get can ask to be answered with a named subset of a tables fields instead of whole rows, which reads only those fields out of an archive rather than deserializing every field of every row it returns |
| F3 | [A three layer performance harness](performance-harness.md) | Criterion micro-benchmarks, ~~the `tmdb` example~~ (a purpose-built workload since [F8](purpose-built-workloads.md)), and a working `hotpath` profile, captured into committed JSON and judged against a frozen baseline and a trailing one, so an optimization can be shown to have worked rather than argued to have |
| F4 | [Archives are validated once, not once per read](validated-archives.md) | A partition read off disk is validated when the read lands and held in a form that carries that fact, so a query against an evicted partition seeks it directly instead of re-running rkyv's validator over the whole buffer first — and the archived read path became reachable from a test for the first time |
| F5 | [The flushed sweep runs on a wakeup, not on every message](flushed-sweep-gate.md) | A shard asks "what is durable now?" when a write has landed or a log is due to rotate, instead of after every message it handles, which took that sweep from 711,638 calls to 21,279 in the same workload without moving when a rotation fires |
| F6 | [A per query stage breakdown](stage-breakdown.md) | Every query records when it reached each of nineteen points between the client's `send` and the response coming back, joined across client and server and reported as the mean breakdown of the queries at each latency rank — which showed the insert tail is `durable_write` and `durable_sync_wait`, and that the two stages the design suspected are 0.6% and 0.8% |
| F7 | [A benchmark runner that renders its own results](bench-runner.md) | The three bash scripts became a workspace crate that runs the benchmarks, records the commit, tree state, machine and toolchain each capture was taken on, compares the macro layer for the first time on whether two observed intervals are disjoint, filters benchmarks the way `cargo test` does, and generates the book's [Benchmark Results](../performance/overview.md) page with its charts and a per-layer staleness badge |
| F8 | [Purpose-built workloads, in the crate that judges them](purpose-built-workloads.md) | The `tmdb` example stopped being the benchmark: fifteen workloads now live in `shoal-bench` itself, each isolating one path, each generating its own rows from a seed so a clean checkout can reproduce the macro layer for the first time — which bought the first per-query service time this repository has produced, a resident-versus-archived control pair, and a fanout curve that puts a number on O13's quadratic term |
| F9 | [Ephemeral tables, and the benchmarks that need them](ephemeral-tables.md) | A table that keeps everything in memory is now declarable in a `#[db]` schema, after two years as a struct nothing could reach — as the same table every persistent database uses with a storage engine that writes nothing, so the two cannot drift apart; which made eight storage-free control workloads possible and let this repository state that durability is roughly 4× the write path rather than infer it from a profile |
| F10 | [Framing and protocol evolution](framing-and-protocol-evolution.md) | Every frame now opens with a version, a message type, two flag bytes and a bounded length, in a module rather than at four hardcoded call sites — which costs zero bytes because narrowing the length to a `u32` pays for the other three fields exactly; a connection opens with a handshake carrying a compile-time fingerprint of the schema each peer was built from, so two peers built from a reordered field refuse each other by name instead of exchanging archives; and a hostile length prefix now closes one connection where it used to panic a shard and every client on it |
| F11 | [The error channel](error-channel.md) | A query that failed can say so, and say which kind of failure it was, instead of answering `Get(None)` — the same answer an empty partition gives: `ResponseAction::Error` carries a pinned code and a line of prose, a frame level `Error` type carries the failures that cannot be a response at all, and between them a read that could not be made, a response too large to frame, and a connection that stopped being read all reach the client that was waiting on them rather than arriving as an empty result, a closed socket, or nothing at all |
| F12 | [Authentication](authentication.md) | A connection can say who it is, and a server can refuse one that will not: SCRAM-SHA-256 over the `Auth`/`AuthResponse` frames [F10](framing-and-protocol-evolution.md) reserved and never wired, negotiated in two bytes cut out of the handshake's reserved tails so nothing about the frame layout moved — the server holds a salted iterated derivation and never a password, an account that does not exist fails in the same words and the same number of round trips as a wrong password, the client checks the server's signature back, and a completed exchange yields the `Principal` that per-table authorization has been filed as blocked on. Off unless a config asks for it |
| F13 | [The transport workloads](transport-workloads.md) | The client is no longer the one layer whose total has never been bounded: eight workloads over the four ways a caller can reach a server — `send` with one query, `send` with many, `stream`, `stream_unordered` — each at a 256-byte row and at a MiB one. The size axis is the point rather than a completeness exercise: at 256 bytes the fixed per-response costs dominate and the four modes spread nearly 4×, at a MiB the per-byte costs dominate and all four collapse into one number, so a per-byte tax such as encryption is invisible in one regime and is the entire cost in the other. This is the precondition [D4](../direction/encryption.md) names for taking encryption at all, and what makes O28 and O30 adjudicable for the first time |
| F14 | [Encryption in transit](encryption-in-transit.md) | TLS 1.3 between a client and a shard, configured per listener and off by default. rustls performs the handshake and then hands the keys to the kernel, so the record layer is kTLS and the response read is **unchanged** — `TcpProxy`, `ShoalResponse` and both server relays did not have to learn that TLS exists, and the zero-copy property the branch is named for survives encryption. [D4](../direction/encryption.md) recommended rustls' unbuffered API instead, on the strength of documentation that says it lets the caller supply the buffers; the crate's source says plaintext comes out of a `Vec` rustls owns, so that option cost exactly the copy it was chosen to avoid. A spike settled the replacement before anything was built. Eight encrypted transport arms pair with the eight plaintext ones, which is the control pair D4 called a precondition — it exists now, and has not been captured |
| F15 | [The client is a crate that cannot start a database](client-server-split.md) | `shoal-core` became three crates: `shoal-proto`, which holds the wire format and the traits a schema implements and links no async runtime at all; `shoal-client`, the tokio client; and the engine. `shoal --no-default-features` is a client with no glommio and no io_uring in its graph, and `shoalctl` — a terminal UI that has no shard, no storage and no partitions, and compiled a storage engine anyway — is now one. The feature that looked like it already did this could never have worked: `pub mod server;` and `shared/traits.rs`'s glommio import were both unconditional, so turning it off produced a build failure rather than a client, and nothing had ever tried. `#[shoal::db(client)]` emits the half of a schema that talks to a database and not the half that is one; `split_by_shard` left the shared trait whose signature named the server's routing types; and generated code names `::shoal::` alone, which closes the body of item 54. Verified by a crate that declares no engine and fails to compile if one creeps back |
| F16 | [The client builder](client-builder.md) | Everything about a client that was a literal is now a setting, and a client can be told about more than one server. `Shoal::builder()` carries pool sizing, deadlines, credentials and TLS on one object, with `PoolConfig` and `Deadlines` defaulting to exactly the six numbers that were hardcoded — a test asserts all six, because making something configurable must not quietly reconfigure the callers who never asked. `ClientOptions` is absorbed rather than replaced, so the four call sites outside the crate do not move. An endpoint now expands to **every** address it resolves to instead of the first one `lookup_host` returned, which is a fix rather than a feature: DNS-based failover, the ordinary way a datacenter names a service, could not have worked. One attempt to connect walks the whole endpoint list from a shared turn counter, and the unit tests pin that rather than the end-to-end behaviour — the integration test passes against a build with the loop stubbed out, because `bb8`'s retries plus the counter find a live server eventually, and "eventually" is bounded by the connection timeout the loop exists to protect. And `client.rs` carries `tracing` spans and `hotpath` scopes for the first time, which is step 0 of the [Direction](../direction/overview.md) chapter and what makes O28 and O30 adjudicable — measured at 144 metrics across sixteen transport workloads with **no result in either direction**, on a build where nothing installs a subscriber, because nothing anywhere called `trace::setup` at the time ([item 69](../appendix/known-issues.md) — the bundled example calls it now, which does not change what F16 measured) |
| F17 | [The workload grid](workload-grid.md) | Seventy-four workloads answering the question the isolating set structurally could not: **what a caller's own workload costs**. Four sweeps over a cross rather than a cube — eleven row widths from 64 B to 4 MiB (three of them declared *distributions* of widths) against all four tables at an even mixture, six read/write shares against all four tables at 1 KiB, three key distributions, and a four-rung load depth ladder. The read/write mixture is the first one anywhere in the repository: the harness had no mix knob at all, because [F8](purpose-built-workloads.md) had deliberately made reads and writes separate workloads so that neither could hide the other. That argument is about **attribution** and it still holds — a regression is attributed with the isolating pairs, never with a grid arm. YCSB's *specification* is adopted natively and its harness is not: `r50`/`r95`/`r100` are workloads A, B and C, the reference row is its 1 KiB record, and the Zipfian and recency generators are YCSB's own algorithms with YCSB's `theta = 0.99` and its FNV-1a scramble — so a number here reads next to a published one while a clean checkout still reproduces everything with nothing fetched. Every arm reports its read and write distributions apart, never pooled, and counts queries as well as rows. Everything a query does — its key, its width, whether it is a read — is a function of its **index** rather than of a generator walked forward, because a mixture is driven by several slots sharing one cursor and anything sequential would depend on which slot got there first |
| F18 | [Results pages that explain themselves](results-pages.md) | The one generated results page had reached 13,617 lines and 1.1 MB, ninety percent of it eighty-eight near-identical charts drawn one per workload in **alphabetical order** — so the newest, narrowest experiment came first and `macro/insert_unsorted` was at line 8,559 — and nothing on it said how to read any of it. It is now ten pages under a `# Performance` section, split by question rather than by workload, at 169 KB. Every page that draws workloads opens with four mandatory blocks: what it measures, how to read it, **what would make it wrong**, and **what it cannot tell you**. Those come from a *family*, and both halves are tests: a workload belonging to no family fails, and a family with an empty block fails. That is the whole mechanism — before it, an unexplained chart was the default outcome of adding a sweep, which is how the page got into the state this replaces. Selection reads the facts a workload recorded rather than parsing its identifier back into values, `render` writes all ten pages or none, and every page still renders from a tree with nothing captured, because `create-missing = false` means a page that declined to render breaks the whole book |
| F19 | [Charts that name their colours in one place](chart-legends.md) | Every line chart named its series just past its last data point, and [F18](results-pages.md) argued that beats a legend because three of the eight series colours fall below 3:1 on the light themes — a chart nobody should have to read by colour. Right about the colour, wrong about what follows: a label at the end of a line identifies that line only while the lines end far apart, and `chart-grid-latency` ends eight curves inside a six pixel band. The pass that pushes colliding names apart ran out of axis, clamped against the plot floor and left two of them three pixels apart at an eight pixel font — which is [item 67](../appendix/resolved/chart-labels.md) again, on a chart that fix could not reach, and a name pushed a third of the way down the chart is not attached to its curve either. So the colour match was being paid for anyway, without the legend that would have made it possible. One `legend` module now serves seven chart modules and replaces **four** copies of the spreading pass, with four separation constants, two pushing down and one pushing up, plus a fourth chart that had none. Reclaiming the gutter those labels needed took a sweep's plot from 509 units wide to 697. Two other things that were unreadable for their own reasons went with it: the encryption charts drew `(tls − plain) / plain` as a percentage, which cannot tell a large share of a very small number from a small share of a large one when the sweep contains both, and are now in nanoseconds against a drawn zero — with the absolute chart widened from one load depth to all four; and every byte axis was a raw integer, so `row-size.md` labelled a tick `1000000` on a chart that called a *series* `1 MiB`. A byte axis now ticks at the widths the workloads were actually run at, in the units they were chosen in |
| F20 | [What each setting is worth](configuration-sweeps.md) | `shoal.yml` carries a dozen tuning knobs and [Configuration](../getting-started/configuration.md) describes all of them accurately — it describes what each setting **is**, and never what value to pick, because nothing here had ever measured one. Every capture ran against the same committed file, so the configuration was a constant rather than an axis, and a bottleneck living in a *setting* was invisible to all four measurement layers at once. Forty-eight workloads under `macro/conf/` now sweep nine knobs: the durability barrier, both filesystem writers' buffer size and queue depth, the intent log size, the shard count, the per-shard memory limit and the frame bound. Each arm is the grid's reference cell — `macro/grid/unsorted/r50/1024` — with **exactly one field** moved, which is [F9](ephemeral-tables.md)'s control-pair shape applied to the configuration and is checked by counting the moved fields of every arm. Each sweep also contains the value `shoal.yml` actually resolves to, measured rather than borrowed from the grid cell it duplicates, so retuning the committed file fails a test rather than quietly leaving the sweep short of the value in use. The generated page's last column is **Real?**, and it is the whole point: a knob whose fastest and slowest arms have overlapping run intervals is reported as no measurable difference however far apart its medians are — the frozen baseline spread 10.5% over five identical runs, so a tuning page without that gate is a table of noise with nine confident recommendations in it. The `throughput_*` sweeps are expected to be flat and are run anyway, because that is [item 71](../appendix/known-issues.md)'s evidence: `throughput_sensitive` reaches the archive *map's* intent log and not the archive writers, which use glommio's defaults. This closes the `Async` against `Fsync` capture [F8](purpose-built-workloads.md) called the cheapest remaining item on the todo page — never run, not because it was hard, but because there was nowhere to put one number comparable to nothing |
| F21 | [Benchmark groups](benchmark-groups.md) | A full capture is four to five hours and F20 adds ninety minutes, so the practical question stopped being "run the benchmarks" and became "run the part that could answer this". [F7](bench-runner.md)'s `cargo test` filtering is the right primitive and a poor way to state an intention: nothing enumerates which prefixes are coherent sets, `macro/conf` selects twice what `macro/conf/storage` does and the difference shows up three hours later, and "everything that isolates one path" is not a substring at all — it is every macro workload *except* the grid, the skew sweep and the configuration sweeps, which is exactly the set somebody wants when attributing a regression. CLAUDE.md had accumulated the recipes as prose, including one it described as "a filter excluding it", which the tool could not do. Twelve named groups now declare those sets over identifier shapes — layers, prefixes, exclusions, and one explicit list for `quick` — in a module that links no engine, because the half of the crate that judges a capture builds with no glommio and `list` has to work there. `--group` is repeatable, combines with or, and intersects with `--layer` and the filters; an unknown name is an error that prints the real ones rather than falling through to selecting everything. `list --groups` prints what each group answers and what it would cost — as a **share** projected onto a named constant, because the measured phases of every macro workload come to two and a half minutes against the four and a half hours the capture takes, and printing that sum as a duration would be wrong by two orders of magnitude. **A group selects and never schedules**: nothing runs concurrently, because two servers at once share a page cache, a device queue and a set of cores, and every number either produced would be a number about the other one as well |
| F22 | [The benchmarks the row size page asked for](row-size-benchmarks.md) | [Row size and what it costs](../tables/row-size.md) named four mechanisms behind the throughput fall past a kilobyte and could isolate none of them, because the axis had a 64× hole in it, ran at one load depth, ran at one mixture, and had no per-stage attribution at more than one width. Its closing section listed the six benchmarks that would settle it. This is all six: a width axis on `wire_codec`, which never varied payload width at all over a fixed thirty byte row; the `latency_buffer` rungs repeated at 8 KiB and 64 KiB, where the step [O34](../appendix/optimizations.md) is about actually lives, rather than at the 1 KiB reference cell whose answer is no; the five widths that close the hole between 8 KiB and 512 KiB; the whole width axis again at `r0` and `r100`, which is what separates a write path per-byte cost from a read path one; a rung at each width with **one** query outstanding, since 32 outstanding 4 MiB queries against sixty four partitions measures a queue; and the stage layer pointed at three widths instead of one, so nineteen stages can be asked which of them grows with bytes. 165 macro arms and 50 micro benchmarks, taking the macro layer from 209 workloads to 374 and a capture from seventy five minutes to about two hours. Every arm is appended rather than spliced, no existing identifier moved, and 140 of the 165 needed no renderer change at all because selection reads the facts an arm recorded rather than its name. **Nothing here has been captured**: the ability to answer the six questions exists and none of them is answered, which every new section on the results page currently says in so many words. Found on the way, and both the same shape — a doc comment describing a guarantee nothing enforced: two attribution layers sharing one artifact path, half [fixed](../appendix/resolved/stage-artifact-overwrite.md) and half [filed](../appendix/known-issues.md); and a configuration knob the page's reading order did not name being [dropped from it](../appendix/resolved/conf-knob-dropped.md) rather than appended, silently, after being captured at full cost |
| F23 | [The intent log's staging buffer sizes itself](self-sizing-staging-buffer.md) | [O34](../appendix/optimizations.md) said a record wider than the staging buffer gets one DMA write and one DMA allocation to itself, leaving the group commit below it nothing to group. `f22-row-size` measured that at **1.22×** on 64 KiB rows and corrected its shape at the same time: the gain is in how many records share an aligned write, not in whether the record fits — a buffer holding *two* 8 KiB records is worth nothing over one holding none, and eight is where it arrives. So the fix is a sizing rule rather than a larger constant. `latency_sensitive.buffer_size` becomes a floor, a new `max_buffer_size` becomes a ceiling, and between them `StreamWriter` sizes each buffer to hold about eight of the widest record the last one held — decaying from the last buffer rather than from a high-water mark, so one outlier costs one oversized buffer instead of pinning every buffer after it wide. A record wider than the ceiling still gets a buffer of its own, because records are never split across buffers and no configured ceiling may override the log's framing. Pinning the ceiling to the floor gives back the old allocation exactly, which is both the escape hatch for an operator with a memory budget and the control the tests are written against. Reproduced before it was fixed: a bundle of 128 rows of 8 KiB became 128 writes and 128 DMA allocations, and is now 16. **Captured** as `f23-staging-buffer`, on the prediction written down before it ran: the five `macro/conf/storage/latency_buffer/r50/w65536/*` rungs converge, since they now all resolve to the same ceiling. They did — the spread across them went 1.225× to **1.010×**, and the shipped `buffer_size: 4096` gained **+22.0%** at 64 KiB rows and +5.6% at 8 KiB on disjoint intervals, with the 1 KiB reference cell unmoved. The fix raised the badly set rungs to where the well set ones already were and left those alone, so what the 1.22× measured was the distance between a good setting and a bad one, and there is no longer a bad one at these widths. Neither `shoal.yml` nor anything under `shoal-bench/` was touched, on purpose: a field added to either would move the fingerprint of every workload and orphan the arms this is measured against |

## How a feature gets written down

Not every change earns a page. A bug fix gets one when it had a wrong mental model behind it; a
feature gets one when it introduced a model that was not there before, because the next person to
touch that code will reason from the model rather than from the diff. The sections are always the
same:

**Context** what was not possible, and why it mattered. **What it does**, across every front end
that can reach it. **Design choices** and **Alternatives rejected**, together, because a design is
only understandable next to what it is not. **Limitations**, so that the gap between what it looks
like and what it does is written down rather than discovered. **Invariants to uphold**, which is
the section to read before changing the code it describes. **Performance**, naming what got
cheaper and what did not. **Tests**, naming what fails if the feature is reverted.
| F24 | [Routing benchmarks](routing-benchmarks.md) | Routing is the hop every query takes between arriving on a socket and reaching the shards that can answer it, and it was the one layer of the query path with **no benchmark of any kind**. [TODOs](../appendix/todos.md) had asked for it since [F3](performance-harness.md) and twice recorded that nothing blocked it — `Ring::find_shard` and `split_by_shard` are pure CPU over plain data, needing no server, no executor and no storage backend — and it was left unbuilt for four more features anyway. Four groups: the key→shard lookup swept across ring sizes, ring construction, the multi-partition split swept across key counts, and a **write** beside it as the control, since a write names one partition whatever the get beside it named and so exercises the same function while being flat in the only thing that varies. Two findings. `find_shard` is **392 ps, flat to 0.4% across a 64× change in the ring** — the tablet ring answering in constant time, which is the whole property [Resolved #11/#12/#37](../appendix/resolved/tablet-ring.md) was built for and which nothing had ever checked. And splitting a get is **quadratic in the keys it names**: `group_by_shard` deduplicates by scanning every key already placed, so the *i*-th key walks the *i*−1 before it — 4.13 ns·n + 0.0109 ns·n², which is 14.5% of the split at 64 keys and 40% at 256. Filed as [O39](../appendix/optimizations.md), and filed with the argument against acting on it: the whole split at 256 keys is 1.77 µs against a read that costs 40, so removing the quadratic outright buys under 2% of the widest query anybody runs. **The thing worth more than either finding** is what it does to a curve already in use — `macro/fanout/n` is what [O13](../appendix/optimizations.md) is ranked on, O13 is *also* an O(n²) in the same caller-set *n* one layer later, so that curve has two quadratics under it and can attribute a bend to neither. Nobody had noticed, because nothing had ever looked at this layer |
| F25 | [Read buffers are filled, not zeroed](read-buffers-are-filled-not-zeroed.md) | Both ends of a round trip allocated a buffer, wrote zeroes over every byte of it, and then overwrote every one of those bytes with the read on the next line — [O29](../appendix/optimizations.md) on the server since [F10](framing-and-protocol-evolution.md) bounded the allocation beside it and left the zeroing, [O37](../appendix/optimizations.md) on the client, unfiled until the read path was walked in code. Neither is written twice now, and the two halves are fixed differently because the two runtimes force it: tokio's `ReadBuf` counts what a reader initialized, so the client's `set_len` is discharged by a **check**; `futures::io::AsyncRead` takes a `&mut [u8]` and reports nothing, so the server's is discharged **structurally** — a `RequestBody` whose field is private and whose only constructor is the read, which is what turns the buffer's initialization state from a property several call sites had to respect into one the type will not let them break. Two claims the entries made turned out to be wrong and are recorded beside them rather than in place of them: the memset was never in the `decode` stage, because `base` is stamped *after* the read and the whole body read therefore falls in `net_in` where no instrument separates it from wire time; and `BytesMut::zeroed` is `vec![0; len]`, which is calloc rather than an unconditional `memset`, so the asymptotic entry is the client's `AlignedVec::resize(len, 0)` and not the server's. Measured by a new pair of criterion groups that run **both shapes in one build**, so one capture adjudicates the change and the `zeroed` arms stay afterwards as controls for a shape neither end has. The capture it needed could not be committed until [Resolved #79](../appendix/resolved/micro-only-capture-current.md): `render` drew all eleven pages from one capture, chosen as the newest with a *micro* layer, so a `--layer micro` capture emptied the seven macro pages — which then said nothing had ever measured a read/write mixture while eleven macro captures sat in the same directory |
| F26 | [Requests are routed from the archive](archive-routed-requests.md) | A bundle of queries arrives as one rkyv archive and the coordinator turned the whole of it into owned Rust values before looking at any of it — [O1](../appendix/optimizations.md), filed against every `String`, `Vec` and filter in every query being allocated and copied out of a buffer that already held them readable. Writing the design pass it was blocked on found it was worse than the entry said: `split_by_shard`'s write arms end in `self.clone()`, and `SortedQuery::Insert { key, row }` carries the row, so **an insert's row was deep-copied twice** — both times on core 0, the single coordinator every request in the system passes through. At the 512 KiB end of the [row-size sweep](../performance/row-size.md) that is a megabyte of memory traffic per insert on the one core that cannot be scaled out of the way. The coordinator now deserializes nothing. It validates the bundle once, reads the partition keys and limit straight out of the archive — scalars sitting inline, no row ever touched — and hands each destination shard the buffer itself, as a `Bytes` whose clone is a refcount. The shard that answers a query is the shard that turns it back into one, and then narrows it. `RequestBody` gains a second and last exit, `freeze`, which keeps [F25](read-buffers-are-filled-not-zeroed.md)'s structural guarantee intact: only a body a read filled can be frozen. `ShoalDatabase::unarchive_queries` — written for exactly this and callerless since it was added — finally has its caller, and became `unsafe fn` on the way, because as a *safe* fn wrapping `access_unchecked` it let any caller hand it any bytes; what discharges it is that the coordinator validated the same immutable buffer first. `split_by_shard` is kept rather than deleted: it is no longer on the live path and it is the only definition of correct the new path has, so every test checks the two against each other and `routing/route_archived/*` is read against `routing/split_by_shard/*`. The per-query deserialize became a twentieth stage, `query_decode`, which is **not** a batch stage — so `decode` is no longer comparable across this change and a reader has to add the two together. Two things stated as limitations rather than discovered later: a fan-out get is now deserialized once per shard instead of once, which is close to a wash and not a saving; and sort-key normalization moved off the coordinator, contradicting a doc comment that said it happened once, which was corrected rather than left standing. **Not captured yet** — the prediction is written down first, and the claim that can fail is that `decode + query_decode` is *less* than today's `decode` on write-heavy arms |
| F27 | [A get's answer carries its partitions, and its rows where they lie](grouped-responses.md) | Two entries shared a rank, a tier and a line of code, and the [optimizations page](../appendix/optimizations.md) was explicit that they had to land together or the wire break would be paid twice: [O2](../appendix/optimizations.md), every returned row copied at least twice — `execute` at ×65 and `reply_serialize` at ×238 over 1 KiB → 512 KiB, together 35% of a wide get — and [O18](../appendix/optimizations.md), a share carrying rows and nothing else, so the shard collecting a split get hashed every row's partition key again to work out something every shard already knew. A get's answer now carries an index of the partitions its rows came from, so the reorder ranks **groups** rather than hashing rows; and a get whose partitions are all resident is serialized inside the table that found its rows, out of the rows themselves. `RowRef<'a, T>` is what makes those two things one change rather than two: its archived type **is** the row's archived type, so a borrowed reply and a copied one write the same bytes by construction rather than by agreement. O18's entry proposed nesting the rows inside their groups and graded itself XL on the strength of it; the flat shape carries the same information and left all **sixty-eight** `access::<T>()` call sites in the tree untouched, so it was M. [O36](../appendix/optimizations.md) fell out of rewriting `finish` anyway, and the `T: PartitionKeySupport` bound coming off the reorder lifted [F2](projections.md)'s own recorded limitation — a projection of a title alone is now expressible. **The finding is bigger than the feature.** A probe on the new path showed the sorted table never reaches it: `check_disk` is set when a partition is created and nothing clears it when storage reports there was never anything on disk, so a sorted partition that has only ever been written to is refused the borrowing path for ever — correctly, and permanently. Nothing failed, because both paths answer identically; it was only visible because somebody checked that the path was taken rather than merely compiled. Filed as [item 80](../appendix/known-issues.md). The archived half is rkyv rather than effort — there is no `Serialize` for an archived value back into its own layout, and the derive cannot recurse into a field type it never sees — and is filed as [O40](../appendix/optimizations.md) rather than left inside O2. `response.rs`, kept orphaned for years as this entry's prior art, was finally opened: twenty-eight lines of struct and `PhantomData`, closing [item 20](../appendix/resolved/orphaned-sources.md) in full. **Not captured** — and the prediction most likely to fail is written down first: the macro layer may not move at all, because the grid's read arms are sorted-table arms |
| F28 | [A get answered off disk is written out of the archive it read](rearchived-rows.md) | [O2](../appendix/optimizations.md)'s other half, and the one [F27](grouped-responses.md) could not take: a partition read from disk stays an archive, and every row it answered was deserialized into an owned row purely so that there was something rkyv knew how to serialize — then written straight back into the layout it had just been read out of. Per heap field per row that is an allocation, a copy archive→owned, a copy owned→output and a free, on the axis O2 is graded on. It was filed as [O40](../appendix/optimizations.md) and graded **XL** because of rkyv rather than effort: **rkyv serializes in one direction only** — no `impl Archive for ArchivedString`, none for `ArchivedVec`, none for any archived type a derive emits — and a mirror would have to recurse through field types `shoal-derive` never sees, since it reads the syntax of a type and a `Vec<Tag>` for a `Tag` in another crate is a name and nothing more. The missing direction is now written: a `Rearchive` trait, an `ArchivedRef<'a, T>` whose archived type **is** `T`'s — the same theorem `RowRef` rests on, arrived at from the other side — and a mirror the derive emits field by field against rkyv's own archived struct. `Option` is served by substituting `ArchivedRef` into rkyv's *own* generic impl, which is the only way it could be: `ArchivedOption`'s tag type is private. What made it M rather than XL is that the fallback is **per field**: a type the derive cannot see inside materializes that one field, and the twenty around it are still written out of the archive, so no schema stops compiling and no row loses more than the one field costs. `#[shoal(rearchive)]` opts a nested type back in. The wire format, `SealReply`, `Answer` and the client are untouched, because the bytes are identical either way. **The finding is again about a path being taken rather than compiling**: a get that parks on the disk read is replayed, and a replayed get copies — so the *first* get after a partition is read pays the old cost and every one after it does not. That is not visible in any test, because both paths answer identically; it came off a probe on `push_archived` beside one on `into_owned`, the discipline [item 80](../appendix/resolved/never-flushed-partitions.md) taught. Filed as [O42](../appendix/optimizations.md). **Captured** as `f28-rearchive`, on predictions written down before it ran, and read against `f27-row-sink` because `compare`'s trailing baseline is from before F27 and re-reports that feature's win as this one's. `partition_sorted/maybe_loaded/get_all` fell **95.5%** at 1024 rows and **95.8%** at 4096, against a `build_all` arm holding the old behaviour in the same build; writing the reply out of an archive rather than copying the rows first is **5.8×**; together about **9× on the answer path** of a wide archived get. The control held — `archived/walk_all` is +1.4% to +2.7%, against the +9.4% it moved in F27's capture. **Two things it cost.** `RowRef` became an enum so one reply can carry both kinds of pointer, and that is +34% to +40% on `build/borrowed` — a regression on the path most gets take, measured in isolation against the parent commit and filed as [O43](../appendix/optimizations.md) rather than netted off. And the capture contains a **+71.5%** on an arm that serializes an owned request bundle and touches nothing here: run alone it is *faster* than the parent commit (7.50 µs against 7.80 µs) where the capture says 17.7 µs, so a widest arm costs twice as much two hundred benchmarks into a capture as it does by itself. That is not a fact about this change, and it is the third capture in a row to show a band nothing can explain — the repeat-capture-at-one-commit the [todos](../appendix/todos.md) have asked for twice is now the cheapest unbuilt thing in the corpus |
| F29 | [An explorer that draws more than one capture](benchmark-explorer.md) | Every chart under `docs/src/performance/` is a static SVG drawn at `render` time from **one** capture, so the question the corpus exists to answer - is this getting better or worse - could not be read off the pages that exist to answer it. Twenty-seven captures are committed and nothing plotted a metric across them; `compare` does it in text, two labels at a time, and [the todos](../appendix/todos.md) had asked twice for the neighbouring thing. A results page is the wrong shape for this and cannot be made the right one, because a chart drawn before anybody has seen it has to choose its axes first. `shoal-bench explore` makes them a control instead: an `egui`/`egui_plot` application over a projection of the whole corpus, with the metric, the axes, the workloads and **an arbitrary set of captures** all chosen at runtime. It opens on throughput against the read share for the newest capture that measured anything - the interactive counterpart of `chart-grid-throughput`, reproduced arm for arm - with no second capture selected, so nothing is compared until somebody asks. The axis is the design: a **sweep** puts a recorded fact on the key axis and one line per capture along it, so ticking a second capture redraws the whole curve beside the first rather than moving one point; a **timeline** puts the captures in order and asks the regression question directly. Neither subsumes the other - a timeline of one capture is a single point. It is a crate, `shoal-top`, and had to be: `shoal-bench` links `walkdir`, whose `same-file` has arms for `unix` and `windows` and none for a browser, so no design where the drawing code reaches back for `family_for` compiles at all. `shoal-bench` enters it with `default-features = false` and gets `serde` structs, so `cargo tree -p shoal-bench --no-default-features` still names no graphics package; it is a workspace **member** rather than an excluded directory so that `cargo check --workspace` still type-checks the UI. **A gap is never a zero**: fifteen hundred of ten thousand possible measurements exist, a line is split into runs of consecutive ones, and a missing bar is not pushed at all - `NAN` is not the marker, because `egui_plot` folds it into the plot bounds and renders nothing. The four mandatory blocks from [F18](results-pages.md) are projected out of `family.rs` and shown under the chart, because freeform selection needs them more than a page whose arms somebody chose. Two things cost an afternoon each and are written down: the wasm build is defeated by the shell's exported `RUSTFLAGS`, which beats every config file, and cargo treats an **empty** target override as no match and falls through; and the `wasm-bindgen` CLI must match the crate exactly, which the lockfile is read for rather than guessed - it had moved to 0.2.127. **The interface has not been seen**: this machine has no display and no `wasm-bindgen`, so everything under the drawing code is tested and the drawing code itself is only compiled. **Amended**: it had no theme at all — no `Visuals`, no preference — so it ran on egui's default of `ThemePreference::System` and the served page's look was decided by whatever `prefers-color-scheme` the reader's browser reported, while `index.html` had all along hardcoded a dark body behind the canvas. It now opens on [One Dark], installed once from the `eframe` creation closure, with One Light behind `global_theme_preference_switch` — and the series palette became **two** arrays of the same eight hue roles in the same order, which is what finally makes `theme.rs`'s standing claim that its hues suit both grounds true rather than a compromise suiting neither. The chart also took the window it was in: the fixed 460-point height is gone and the `ScrollArea` that forced it is gone with it, the picker collapses to a toggle, and the four mandatory blocks moved to a bottom panel **shut on first sight** — four blocks for the default family wrap taller than the chart above them, so left open they were most of the page. Folding is not dropping: the header still names the family, and the caveat about a selection spanning several of them stays **above** the fold, because it is the one line a reader can be harmed by not seeing |
| F30 | [Only comparable axes share a chart](plot-axis-units.md) | [F29](benchmark-explorer.md) made the axes a control and never asked whether the workloads a reader ticked belonged on one chart. They usually do not: the corpus holds three hundred and seventy-four of them, driving different paths, at two scales, timed two ways, over rows whose width is sometimes a measurement and sometimes a mean — and every one of them was folded into **one line per capture**. The clearest case was the explorer's own opening view, which drew twenty-four grid arms across four tables as a single zigzag where [the grid page](../performance/grid.md) draws four lines; filed as [item 82](../appendix/resolved/one-line-per-capture.md) and reproduced before it was fixed. Two of the refusals that would have caught the rest were **documented and never written**: the doc comments on `ScaleFactsLite::scale` and `Timing` both claimed `Source::series` refused to join them, and neither field was read anywhere in the drawing crate. The rule is now **axis units, and units alone** — the metric's unit, refined by the named scale and, for a percentile only, by whether its samples were stamped per batch or per query; and the key axis's quantity, which is *absent* for a workload with no position on it and which splits a row width in two, because a declared distribution reports a **mean** and `row_size.rs` already says why a mean has no place on an axis of measurements. Deliberately weaker than *they measure the same thing*: an insert loop and a mixture are both queries a second and comparing them is a real question. Matching units is what makes a selection *allowed*, not what makes it one line — a sweep now draws one series per **(capture, curve)**, a curve being the facts a caller *set* minus whichever the axis reads, each named by what separates it from the others and nothing else. **`rows` and `keys` are excluded from that key** and there is a test saying so: a workload sizes them from the row width against a byte budget, so holding them gives the width sweep fifty-two curves of one point and no line at all. ~~Hue now carries the curve and dash the capture, which is what `theme.rs` had always claimed and the plot had never done.~~ Half of it: the dash half is right and unchanged, and the hue half only ever meant the curve's *position* among the ones drawn — [F32](chart-line-identity.md) makes it the **table**, and adds a marker for which of that table's curves a line is. A workload in other units is **disabled in the picker rather than hidden**, with the reason on it — the first refusal in a tool whose house rule is *warn, never refuse*, and it is a departure on purpose: `incomparable` draws two incomparable captures because the numbers exist on one axis, and here there is no axis to draw badly on. The two charts it was built against are buttons: `chart-grid-throughput` and `chart-row-size-ops`, each selecting its arms by recorded facts and family and never by parsing an identifier, each asserted against `arms::grid` on the **real corpus** so the "reproduces it arm for arm" claim is checked rather than asserted — four curves of six points and four of thirteen. `log_x` arrived with the second one, because sixteen widths from 64 B to 4 MiB is six decades, and it defaults off because the read share runs from zero, which no logarithmic axis can place |
| F31 | [A metric a workload can actually answer](metric-availability.md) | The explorer's metric box offered thirty three entries — five whole-workload numbers plus every operation name anywhere in the corpus crossed with seven ranks — narrowed by nothing, so finding the live one for whatever was ticked was guesswork. Underneath it, `Index::units` decided comparability from the metric's *unit* and the measurement's scale and timing and **never read the measurement**, so no gate anywhere asked whether the number existed. Three metrics are universal across the 1521 committed measurements; two are absent for 88 of the 374 workloads, and the latency metrics partition the corpus outright — `read`+`write` for 148 workloads, `get` for 85, `read` for 75, `write` for 64. One of the two absent ones is `OpsPerSec`, which the explorer **opens** on ([item 83](../appendix/resolved/default-metric-half-the-corpus-cannot-answer.md)). Now `units` reads the measurement, which makes `refusal`, `reconcile`, `anchor`, `select_all`, `in_one_unit` and `Source::series` metric-aware from one line; the control is three lists — kind, then operation and rank under `latency` alone — each holding the **intersection** of what the ticked workloads answer, so a dead choice is not offered rather than being offered and drawing nothing; and the picker shows only what can be drawn, families included, reversing [F30](plot-axis-units.md)'s *disabled rather than hidden* on the grounds that greying out three hundred of three hundred and seventy-four rows is not a tree anybody can read. Nothing is dropped silently: every header carries how many it is hiding and a line under the tree totals them with both reasons |
| F32 | [A colour per table, and a chart that frames itself](chart-line-identity.md) | Three things about the explorer's chart that decide what a reader takes off it rather than which numbers are on it. **The colours meant a position, not a thing**: `theme::series` was indexed by the curve's position among the ones drawn, so the sorted table was blue alone and orange with the unsorted one ticked ahead of it - while the module doc had claimed since [F29](benchmark-explorer.md) that *hue carries the series - the table, or the workload*. **Two workloads whose recorded facts agree drew as one line** ([item 84](../appendix/resolved/identical-facts-one-line.md)): the two keyed gets are one struct with a `Residency` field that reaches no `ScaleFacts`, so both answered to one curve key and drew as a vertical segment joining a memory hit to a disk read. **The chart stayed framed for whatever it had been framed for** ([item 85](../appendix/resolved/chart-framed-for-the-last-selection.md)): `egui_plot` keys its `PlotMemory` on the plot id, the id is a constant, so the `auto_bounds = false` any scroll writes outlived every change to the metric, the axes and the selection - a chart framed on a rate and handed a p99 drew empty, and the only way out was an undocumented double click. Identity is now **three channels**: hue is the **table**, at its fixed slot in `index::TABLE_ORDER` so all four are reserved whether or not a chart draws them and a table's colour is a property of the table; the dash pattern is the capture, on a sweep, which keeps [F30](plot-axis-units.md)'s comparison intact; and a marker is which of that table's curves this is. A timeline has no single capture to encode, so there the mark carries both and walks the five markers before it moves the dash. `Series::group` is gone and `hue`/`mark` replace it. The fold is fixed **downstream of the curve key rather than inside it**: `split_curves` splits a curve whose members share a position on the axis into one line per workload, each named by the identifier that is the only thing separating them, and leaves every other curve whole - putting the identifier in the key instead would have broken both invariants item 82 states, giving a width sweep sixteen curves of one point. And the chart frames itself whenever `Explorer::framing` moves - captures, workloads in tick order, metric, key axis, chart kind, both log toggles - with a `fit` button for the other case, so a reader's own zoom sticks until the next thing that changes what is on screen rather than until they notice it has stopped meaning anything. **The interface has still not been seen**: `stroke_of` is tested directly and the markers landing where they are meant to on a real canvas is checked by compiling against `egui_plot` 0.37's signatures and by nothing else |
| F33 | [The chart answers where you point, and the capture list says whether it can](chart-readout.md) | Two questions the explorer could not answer. **What is the number here?** `plot::draw` never installed a `label_formatter`, so hovering ran `egui_plot`'s own - which reports the **plotted** number, so a curve at 412,500 queries a second reads `y = 5.615` the moment `log value axis` is on; names the series with the **empty string**, because [F29](benchmark-explorer.md) names only a line's first run so the legend keeps one entry per series rather than one per gap, and gaps are the common case; and answers for **one line**, where the question the crate exists for is which capture is ahead at this key. All three are [item 86](../appendix/resolved/hover-label-reads-in-decades.md), and the fix is to refuse the label rather than reformat it: a formatter is handed one item, so it can never answer for the rest of the chart, and the empty name is not recoverable from inside it. The pointer now selects a **column** - one position on the key axis - and `readout.rs` lists every drawn line's value in it, largest first, absent last and said as the word `absent`, each written with the same `fmt::value` the ticks are written with, from `Series::points`, which holds measurements rather than positions. `readout::columns` becomes the only definition of where a column is and `plot::key_ticks` was rewritten onto it, because two pieces of that arithmetic drift into a column captioned with its neighbour's key - which is unreadable and looks exactly like a correct chart. **Would ticking this capture draw anything?** [F31](metric-availability.md) built the notion of a measurement that carries no number and applied it to workloads alone, while five committed captures carry no macro layer at all and the latency metrics partition the corpus outright. `Index::captures_answering` asks it of a capture, judged against the **ticked** workloads - or against everything it measured when nothing is ticked, which is the rule `metrics_for` already follows - and a capture that answers nothing is greyed with the reason on it and the total under the list. **Greyed, not hidden**, which is F31's rule reversed on purpose: a capture is an identity a reader knows by name out of twenty-seven, so one disappearing reads as a corpus that lost a run, where a workload disappearing out of three hundred and seventy-four reads as a filter working. It stays tickable, because ticking is how a reader lines a capture up before moving the metric onto one it answers. **The interface has still not been seen** - this machine has no display, so the swatches, the rule down the column and the greyed rows are compiled for both targets and tested underneath |
| F34 | [A capture that honors its tracing config](benchmark-tracing.md) | `shoal-core/src/server/trace.rs` had built a complete OTLP over HTTP pipeline for a long time and `setup` had **one caller in the workspace**, the bundled example - so `shoal-workload`, the process that runs all three hundred and seventy-four workloads, installed no subscriber, the `tracing:` section of `shoal.yml` configured nothing for a capture, and every `#[instrument]` and `event!` the engine and the client reach dispatched to `NoSubscriber` ([item 69](../appendix/resolved/benchmark-tracing.md)). The immediate reason to close it is judging **Grafana as a benchmark viewer** against the explorer ([F29](benchmark-explorer.md)), which needs the corpus in a collector. **The config is the switch** - there is no `--trace` flag, because a capture already takes `--conf` and a second switch that can disagree with the file is a second thing to get wrong. `trace::setup_with` takes a `TraceOptions` carrying a service name, resource attributes and a console stream; a workload reports as `shoal-workload` rather than `Shoal` so a capture's spans and a deployment's are two series, and carries `shoal.workload`, `shoal.label`, `shoal.scale`, `shoal.seed` and `shoal.port` on every span. `harness/conf.rs` stopped forcing `tracing.level = Warn`. Two settings arrived because the pipeline was unusable at benchmark volume without them: `sample_ratio`, wired to `Sampler::TraceIdRatioBased`, and an `OtlpMetrics` sink that `Tracing::metrics_sink()` derives from the trace endpoint by swapping `/v1/traces` for `/v1/metrics`, because a collector serves both on one host and requiring the URL twice is requiring two places to forget. **`tracing.level` is the cost knob and `sample_ratio` is the collector's**, which is easy to get backwards: a sampler decides *after* the span is built, and `#[instrument]` defaults to `INFO`, so `level: Info` puts a registry slab insert per query on three per-query callsites - the cost [F5](flushed-sweep-gate.md) counted at 711,638 inserts per run for one span it then removed. The committed `shoal.yml` names `Warn`. Live metrics are recorded **per run from the finished artifact**, not per query: an instrument on `Measurement::record` would make a traced capture slower by a property of the metrics module rather than of the database, so `Meters::record` takes the `MacroCaptureV2` after the server has stopped - `shoal_bench.run.completed` counted against `workload_ids::IDS` is capture progress, and needed no runner-side change. The console layer writes to **stderr**, which is not cosmetic: the runner harvests the hotpath profile from the last line on stdout, so a log line there corrupts `<label>.hotpath.json` silently. `ConfFacts` gained `trace_level` and `trace_remote`, and `compare` - which read no field of `ConfFacts` at all before - names a pair that traced differently rather than comparing them in silence, while staying quiet when neither side recorded the facts so the pre-F34 corpus stays comparable with itself. **Nothing here is measured**: no capture was taken, and what tracing costs at each level is quoted from F5 by analogy |
| F35 | [A trace context on the wire](wire-trace-context.md) | [Resolved #89](../appendix/resolved/fragmented-query-traces.md) made every span the *server* opens for one query into one trace and **stopped at the socket on purpose**: the client's spans were a second trace in a second process, and nothing on the wire said the two belonged together. So the one question a slow query raises - was that the disk or the pool? - could be asked of neither trace, because neither contained the other. The shape is the one [Todos](../appendix/todos.md) had already picked over the alternative: **a `Flags` bit plus a widened request preamble**, and `PROTOCOL_VERSION` 2 → 3 with it. `Flags::TRACE_CONTEXT` is bit 4, the first of the twelve free ones to be spent, and it says a 26 byte block follows the header - a version byte, a 16 byte trace id, an 8 byte span id and the W3C trace flags. The rejected shape, a field on `Queries<S>`, costs the same flag day and puts the context **inside the archive every shard reads on the hot path** rather than in a preamble the relay reads once; the flag bit is also what makes the *next* optional block a call site rather than another version bump. **The context is a read of its own and never the front of the body buffer**, which is the constraint that decided the whole layout: the payload is an rkyv archive accessed in place and `RequestBody::read_from` sizes its allocation exactly, so 26 bytes in front of it would misalign every pointer in the bundle - `decode_request` therefore stops at the header, and `RequestPreamble` holds one fixed size buffer for both shapes so the client's write stays two `IoSlice`s. `Shoal::request` keeps its `parent: None`: the parent this sets is an **OpenTelemetry** parent resolved by the OTLP layer, and the registry's is a separate mechanism that has never heard of the other process. **A context that names no parent is never built** - `TraceContext::new` refuses an all-zero id at both ends - and that refusal is the feature rather than a tidiness check, since `tracing` turns an unresolvable parent into `Attributes::new_root`, which is a **new trace** and not an orphan anybody would notice. The return half needed the same move the server makes with `QueryMetadata.span`: a query's answers arrive in a detached reader shared by every query on the connection, so `Waiter` parks the span the send opened and `Shoal::response` and both `next()`s hang off it - three client spans this page's own list had twice claimed and never had. **The client's half is behind an `otel` feature, off by default**, which is safe to gate only because the *reading* side is unconditional: a server understands the flag bit whether or not anything it serves ever sets one. **The sampling decision moved to the client** - the context is adopted as a *remote* parent, which a parent based sampler defers to, so `sample_ratio` on a server now governs only traces that arrived without one. **Nothing here is measured**: no capture was taken, no workload fingerprint moved, and the two `INFO` spans per response this puts on a per-query client path are filed as [O45](../appendix/optimizations.md) rather than waved through |

[One Dark]: https://github.com/atom/atom/tree/master/packages/one-dark-ui
| F36 | [The harness and the facts](cluster-harness.md) | The first milestone of [Distributed Shoal](../distributed/overview.md), [M0](../distributed/milestones.md#m0-step-0-the-harness-and-the-facts), and it builds nothing distributed: it builds what every later milestone is judged by. **`shoal-model`**, a pure crate with no runtime and no engine, models one Raft-shaped tablet group against the contract's five positions, with a `Policy` whose safe setting is [P1–P6](../distributed/protocol.md#the-contract) and whose six unsafe settings are the violations the contract table says must be rejected; a checker that computes commitment from stable logs and fsync terms alone, never from a node's own opinion; a ledger and a per-key oracle holding successful, rejected and unknown outcomes to their three contracts; and schedules that are generated from a seed, written by hand, saved as JSON, replayed and minimized — seven saved, each replaying to the violation it records, the B=100/C=101/A+B=102 schedule among them. **The cluster fixture** under `shoal/tests/cluster/` starts real servers and mock peers as re-executed children on port zero, waits for the endpoints each bound, cuts and heals directed links a reconnect cannot route around, pauses and kills, allocates whole physical cores and records sharing, and cleans up on a panic. **The benchmark groundwork**: a `ClusterFacts` record absent from every single-node capture and named by `compare` rather than compared across, a frozen `docs/perf/ports.json` a test holds every workload to, a cluster port allocator above the single-node range, `shoal-workload serve` and `run --server` as the separate load driver, and the explorer's mirror made total — which found two F34 fields it had never carried. First it needed [Resolved #38, 58, 88](../appendix/resolved/pool-readiness.md): `ShoalPool::ready`, `failure` and `bound_addr`, with port zero resolved by a reuse-port reservation the pool holds until every shard has bound, so no test chooses a port or sleeps any more. **Nothing here is a capture**: the matched single-node arm, run in process and through `--server`, produced byte-identical facts, which is the exit criterion; and the honest `exit` surfaced [item 91](../appendix/known-issues.md#91-a-compaction-that-fails-ends-the-compactor) |
